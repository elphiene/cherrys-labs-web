{
  "name": "cherrys-labs",
  "version": "1.0.0",
  "private": true,
  "description": "Cherry's Labs — showcase site for cherry + el. Static HTML/CSS/JS, no build step.",
  "scripts": {
    "start": "npx --yes serve . -l ${PORT:-3000}",
    "build": "echo 'static site — no build step'",
    "deploy": "wrangler pages deploy ."
  }
}
